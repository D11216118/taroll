# 塔羅單檔網頁（index.html）軟體開發規格

## 1. 專案概述
本專案製作一個**單檔（Single File）HTML** 的塔羅抽牌小工具，目標是：
- 不需後端、不需建置工具、可離線使用
- 提供基本 UI（問題輸入、抽牌按鈕、結果區）
- 支援**隨機抽牌**（以 78 張塔羅為牌庫，至少涵蓋名稱與類別）
- 顯示抽到的牌與**簡單敘述**（正位/逆位的簡短解讀）

交付物：`index.html`（單一檔案，內含 HTML/CSS/JS）與本規格文件 `Spec.md`。

---

## 2. 使用者故事（User Stories）
1. 使用者可以輸入想詢問的問題並抽一張牌得到當下建議。
2. 使用者可以不輸入問題也抽牌（作為每日提醒/隨機提示）。
3. 使用者能看到抽到的牌名、正位或逆位、簡短解釋文字。
4. 使用者能再次抽牌，得到新的結果（同一輪只抽 1 張，無需避免重複）。

---

## 3. 功能需求（Functional Requirements）

### 3.1 基本 UI
**畫面元件**
- 標題：如「塔羅抽一張」
- 問題輸入框（textarea 或 input）
- 按鈕：
  - `抽牌`：執行隨機抽牌與顯示結果
  - （可選）`清除`：清空問題與結果
- 結果區顯示：
  - 抽牌時間（本機時間）
  - 問題（若有輸入）
  - 牌名
  - 正位/逆位（50% 機率）
  - 簡短敘述（由內建牌義取得）

**互動行為**
- 點擊 `抽牌` 後，更新結果區，覆蓋前一次結果
- 若問題未填，仍可抽牌，結果區顯示「（未填寫問題）」或直接略過

### 3.2 隨機抽牌
- 牌庫至少包含：
  - **78 張牌名**（建議完整：22 大阿爾克那 + 56 小阿爾克那）
  - 每張牌至少有：
    - `id`
    - `name`
    - `arcana`：`major` 或 `minor`
    - `upright`：簡短正位敘述（1~2 句）
    - `reversed`：簡短逆位敘述（1~2 句）
  - 小阿爾克那可選擇補充：
    - `suit`：`wands/cups/swords/pentacles`
    - `rank`：`ace..10/page/knight/queen/king`

- 抽牌流程：
  1. 從牌庫中隨機選 1 張
  2. 隨機決定正位/逆位（預設 50/50）
  3. 根據正逆位選擇對應敘述並渲染至 UI

### 3.3 簡單敘述（解牌）
- 顯示文字來源為內建資料（非 AI、非外部 API）
- 內容以「提示/建議」為主，避免絕對化預言
- 顯示格式建議：
  - `建議：...`
  - `提醒：...`
  - `焦點：...`

---

## 4. 非功能需求（Non-Functional Requirements）
- **相容性**：Chrome / Edge / Safari 最新版
- **可攜性**：單檔 `index.html` 可直接雙擊開啟
- **效能**：抽牌操作 < 100ms（在一般裝置上）
- **可讀性**：JS 模組化（即使在同檔中也要分區塊、加註解）
- **無外部依賴**：不使用 CDN 套件（可選：允許使用系統字體）

---

## 5. 資料設計（In-Memory Data Model）
在 `index.html` 內以常數陣列保存：

```js
const TAROT_CARDS = [
  {
    id: "m00",
    name: "愚者 The Fool",
    arcana: "major",
    upright: "新的開始、嘗試與自由。保持好奇，但別忽略基本準備。",
    reversed: "衝動或逃避現實的傾向。先穩住步伐再前進。"
  },
  ...
];
```

> 牌義為簡短版（MVP），可逐步補齊 78 張完整敘述。

---

## 6. UI/版面規格（簡版線框）
- 上方：標題 + 副標（可選）
- 中間：
  - 問題輸入框
  - 抽牌按鈕
- 下方結果卡片：
  - 時間
  - 問題
  - 抽到的牌（大字）
  - 正/逆位標籤
  - 解讀文字（段落）

---

## 7. 程式設計（Implementation Notes）

### 7.1 建議檔案結構（單檔）
`index.html` 內分段：
1. HTML 結構（UI）
2. CSS（基本排版、卡片樣式、按鈕）
3. JS
   - 常數牌庫 `TAROT_CARDS`
   - `randomInt(max)`
   - `drawCard()`
   - `renderReading(reading)`
   - DOM 事件綁定

### 7.2 核心演算法（抽牌）
- `card = TAROT_CARDS[randomInt(TAROT_CARDS.length)]`
- `orientation = Math.random() < 0.5 ? "upright" : "reversed"`
- `text = orientation === "upright" ? card.upright : card.reversed`

### 7.3 錯誤處理
- 若牌庫為空：結果區顯示錯誤訊息「牌庫資料缺失」
- DOM 元件不存在：在 console 顯示錯誤並中止（避免白畫面）

---

## 8. 驗收標準（Acceptance Criteria）
1. 開啟 `index.html` 可看到：標題、問題輸入、抽牌按鈕、結果區。
2. 點擊抽牌後，結果區必定顯示：
   - 牌名
   - 正位或逆位
   - 對應的簡短敘述
   - 抽牌時間
3. 不輸入問題也能抽牌，程式不報錯。
4. 連續抽牌會更新結果，且牌名有機率改變（確定是隨機）。
5. 全程不依賴外部網路資源也可運作（離線可用）。

---

## 9. 延伸功能（非本期，Backlog）
- 三張牌牌陣（過去/現在/未來）
- 花色比例摘要（權杖/聖杯/寶劍/錢幣）
- 抽牌紀錄（localStorage）
- 可切換語言（中文/英文）
- 牌卡圖片與翻牌動畫

---

## 10. 交付清單
- [x] `Spec.md`（本文件）
- [ ] `index.html`（單檔：基本 UI + 隨機抽牌 + 簡單敘述；內含 HTML/CSS/JS）
